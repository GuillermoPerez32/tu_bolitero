import 'package:flutter/material.dart';

class AlgoritmosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Algoritmos'),
      ),
      body: Center(
        child: Text(
          'Algoritmos Screen',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
