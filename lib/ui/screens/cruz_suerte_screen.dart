import 'package:flutter/material.dart';

class CruzSuerteScreen extends StatelessWidget {
  const CruzSuerteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cruz de la suerte'),
      ),
    );
  }
}
