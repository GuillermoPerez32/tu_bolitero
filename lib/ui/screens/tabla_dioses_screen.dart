import 'package:flutter/material.dart';

class TablaDiosesScreen extends StatelessWidget {
  const TablaDiosesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tabla de los Dioses'),
      ),
    );
  }
}
